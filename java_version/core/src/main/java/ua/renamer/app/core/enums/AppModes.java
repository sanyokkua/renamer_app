package ua.renamer.app.core.enums;

/**
 * An enumeration representing different modes of operation for the application.
 */
public enum AppModes {
    ADD_CUSTOM_TEXT,
    CHANGE_CASE,
    USE_DATETIME,
    USE_IMAGE_DIMENSIONS,
    USE_PARENT_FOLDER_NAME,
    REMOVE_CUSTOM_TEXT,
    REPLACE_CUSTOM_TEXT,
    ADD_SEQUENCE,
    TRUNCATE_FILE_NAME,
    CHANGE_EXTENSION,
}
