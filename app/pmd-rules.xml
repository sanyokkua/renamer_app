<?xml version="1.0"?>
<ruleset name="RenamerApp Rules"
    xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0
    https://pmd.sourceforge.io/ruleset_2_0_0.xsd">

    <description>
        PMD rules for the Renamer App (analyzed as Java 21, source is Java 25).
        Tier 1 (mandatory): errorprone, bestpractices, security.
        Tier 2 (selected): multithreading (virtual threads used), cyclomatic complexity.
        failOnViolation=false in local code-quality profile — violations are informational.
    </description>

    <!-- TIER 1: Always Enabled (Critical) -->

    <rule ref="category/java/errorprone.xml">
        <!-- NullAssignment used in some cleanup patterns -->
        <exclude name="NullAssignment"/>
        <!-- MissingSerialVersionUID not relevant — no Serializable classes in this project -->
        <exclude name="MissingSerialVersionUID"/>
    </rule>

    <rule ref="category/java/bestpractices.xml">
        <!-- GuardLogStatement conflicts with SLF4J parameterized logging pattern -->
        <exclude name="GuardLogStatement"/>
    </rule>

    <rule ref="category/java/security.xml"/>

    <!-- TIER 2: Selected Rules -->

    <!-- Multithreading — relevant: project uses virtual threads -->
    <rule ref="category/java/multithreading.xml">
        <exclude name="UseConcurrentHashMap"/>
        <exclude name="DoNotUseThreads"/>
    </rule>

    <!-- Complexity threshold — methods over 15 are flagged -->
    <rule ref="category/java/design.xml/CyclomaticComplexity">
        <properties>
            <property name="methodReportLevel" value="15"/>
            <property name="classReportLevel" value="80"/>
        </properties>
    </rule>

    <!-- Structural code style -->
    <rule ref="category/java/codestyle.xml/UnnecessaryLocalBeforeReturn"/>
    <rule ref="category/java/codestyle.xml/UnnecessaryModifier"/>

    <!-- Exclusions for generated sources -->
    <exclude-pattern>.*/target/generated-sources/.*</exclude-pattern>
    <exclude-pattern>.*/target/generated-test-sources/.*</exclude-pattern>

</ruleset>
