package ua.renamer.app.core.enums;

/**
 * An enumeration representing different positions for item replacement.
 * It includes BEGIN, END, and REPLACE options.
 */
public enum ItemPositionWithReplacement {
    BEGIN,
    END,
    REPLACE,
}
